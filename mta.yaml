_schema-version: "3.2"
ID: ui5project04
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: ui5project04-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: ui5project04-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - ui5project04.zip
      name: ui5project04
      target-path: resources/
- name: ui5project04
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: ui5project04-approuter
  type: approuter.nodejs
  path: ui5project04-approuter
  requires:
  - name: ui5project04_html_repo_runtime
  - name: uaa_ui5project04
  - name: ui5project04-destination-service
  
  parameters:
    disk-quota: 256M
    memory: 256M

resources:
- name: ui5project04-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: ui5project04-html5-srv
    service-plan: app-host
- name: ui5project04-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: ui5project04-destination-service
    service-plan: lite
- name: ui5project04_html_repo_runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: uaa_ui5project04
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: ui5project04-xsuaa-service
    service-plan: application
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
